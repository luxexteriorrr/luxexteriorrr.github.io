# OS files
.DS_Store

# Project data (not for publishing)
projects-data.xlsx

# Plan docs
PLAN.md

# Dependencies (rebuild with: npm install)
node_modules/

# Vestige CNC relief: the full 368 MB scan stays local (a symlink into .git/lfs);
# the page uses the 71 MB meshopt copy, which is committed as a normal file.
vestige/assets/3d/*
!vestige/assets/3d/cnc1-meshopt.glb
